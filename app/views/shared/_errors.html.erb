<% if resource.errors.present? %>
  <div>
    <p><%= pluralize(resource.errors.count, 'error') %> detected:</p>

    <ul>
      <% resource.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>