<h1><%= t('.header') %> "<%= @question.test.title %>"</h1>

<p><%= @question.body %></p>

<h2><%= t('.answers') %></h2>

<table class="table">
  <%= render @question.answers %>
</table>

<p><%= link_to t('helpers.submit.answer.new'), new_admin_question_answer_path(@question), :class => 'btn btn-primary' %></p>

<%= link_to t('helpers.page.back'), admin_test_path(@question.test), :class => "btn"  %>